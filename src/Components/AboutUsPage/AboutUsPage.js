import { render } from '@testing-library/react';
import React, {Component} from 'react';

class AboutUs extends Component{
    render() {
        return(
<div className="full">AboutUs</div>

        );
    }
}

export default AboutUs;